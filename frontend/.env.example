# ─── Required for external deployments (Vercel, Railway, etc.) ───────────────

# The full base URL of your deployed API server, including the /api prefix.
# Example: https://your-api.railway.app/api
VITE_API_URL=https://your-api-server.railway.app/api

# ─── Replit-managed (set automatically on Replit, not needed elsewhere) ───────
# PORT=        (auto-assigned by Replit)
# BASE_PATH=   (auto-assigned by Replit, defaults to "/" elsewhere)
